interface:
  display_name: "Codex 実装"
  short_description: "tasks を実装して都度検証"
  default_prompt: "この skill を使って .agents/context/tasks.md の次のタスクを実装し、各変更後に検証してください。plan / implementation drift を見つけたら先に audit し、verify wrapper が壊れている場合は failure mode を残して直接検証へ切り替えてください。"
